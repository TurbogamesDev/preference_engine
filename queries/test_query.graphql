query ($type: MediaType!, $userName: String!, $status: MediaListStatus) {
  MediaListCollection(type: $type, userName: $userName, status: $status) {
    lists {
      name
      entries {
        media {
          title {
            english
          }
          episodes
        }
        progress
      }
    }
  }
}